{
  "name": "directus-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build:directus",
    "build:directus": "turbo run build:directus",
    "start:directus": "turbo run start:directus",
    "start": "turbo run start:directus",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.10.2",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": "~8.10.2"
  }
}
